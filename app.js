// Step 1: nothing to do yet. This log exists to prove the file is actually
// wired up and loading — so that when step 3 adds Supabase, a silent failure
// can't be blamed on the script never running.
console.log("app.js loaded");
